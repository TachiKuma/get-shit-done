# Milestone v2.0: GSD-CN 中文发行版基线

**Status:** ✅ SHIPPED 2026-04-24
**Phases:** 19-20
**Total Plans:** 7
**Audit:** passed (.planning/milestones/v2.0-MILESTONE-AUDIT.md)

## Overview

v2.0 将项目主线正式切换为 GSD-CN：上游 get-shit-done 的非官方简体中文发行版与兼容层。该 milestone 锁定了 zh-CN 第一阶段范围、gsdcn 用户可见命名空间、.planning-gsdcn 状态根、官方 GSD / GSD-CN 同机共存与同工作区切换边界，并在 Phase 20 补齐安装 brand 激活与 E2E 证据链。

## Phases

### Phase 19: GSD-CN 产品定义与兼容边界锁定

**Goal**: 将项目正式重定义为 `GSD-CN` 中文发行版，锁定第一阶段范围、`gsdcn` 用户可见入口命名规则、上游行为兼容边界与官方 GSD 共存约束，为后续 discuss/planning 提供稳定规格基线

**Depends on**: Phase 15 delivered localization foundation; superseded v1.3 sync context remains reusable background

**Requirements**: CN-01, CN-02, CN-03, CN-04, CN-05, CN-06, CN-07

**Success Criteria** (what must be TRUE):
  1. `.planning` 活跃文档明确以 `GSD-CN` 中文发行版作为唯一主线，不再将“多语言本地化框架”视为当前目标
  2. 第一阶段范围被锁定为 `zh-CN` 中文发行版，其他语言与 CCB 特化被明确标记为 deferred / out of scope
  3. 所有用户可见入口的 `gsdcn` 前缀要求被明文化，覆盖命令、skills、workflows、tools、安装入口、文档示例与生成产物引用
  4. 官方 `GSD` 与 `GSD-CN` 的同机共存、同工作区切换与互不覆盖约束被写成硬 requirement
  5. 行为一致性边界被写成硬 requirement：仅允许显示文案、注释、默认语言、命令前缀变化
  6. `19-SPEC.md` 已存在，并足够支持 `/gsd-discuss-phase 19` 继续讨论“如何实现”

**Plans**:
- [x] `19-01-PLAN.md` — planning-root substrate：为 `GSD-CN` 建立 `.planning-gsdcn` 状态根，并让 CLI / SDK / workstream / project 路由保持一致
- [x] `19-02-PLAN.md` — installer/runtime namespace：落地 `gsdcn` 前缀、安装级命名空间隔离与同机共存
- [x] `19-03-PLAN.md` — README/docs/help surfaces：切换中文主叙事、`gsdcn` 用户入口与第一阶段 `zh-CN` 边界说明
- [x] `19-04-PLAN.md` — parity/coexistence regression gate：建立 focused blocker suite（28 tests），并刷新 inventory/manifests；focused full suite 178 tests pass

**UI hint**: no

---

### Phase 20: GSD-CN install-brand E2E 闭环与 surface 残留清理

**Goal**: 关闭 v2.0 milestone audit 发现的 GSD-CN 安装入口、brand 激活、状态根 E2E 与 runtime surface 残留缺口，确保用户按中文文档安装和使用时实际进入 `gsdcn` brand、生成隔离安装产物，并将 GSD-CN 命令状态写入 `.planning-gsdcn`。

**Depends on**: Phase 19 delivered GSD-CN product boundary, planning-root substrate, installer namespace substrate, docs/help surfaces, and parity/coexistence gate

**Requirements**: CN-03, CN-04, CN-05, CN-07

**Gap Closure**: Closes `.planning/v2.0-MILESTONE-AUDIT.md` gaps `GAP-19-INSTALL-BRAND-ACTIVATION`, `GAP-19-STATE-ROOT-E2E`, and `GAP-19-CLINE-SURFACE`.

**Success Criteria** (what must be TRUE):
  1. README / install entry gives users an explicit, tested path that activates `GSD_BRAND=gsdcn` or an equivalent dedicated GSD-CN brand mechanism.
  2. Installer E2E/fixture proves the documented GSD-CN install path creates `gsdcn-*` assets, `gsdcn-file-manifest.json`, brand-aware update hook config, and does not overwrite official GSD artifacts.
  3. Command execution E2E/fixture proves GSD-CN user commands write active project state under `.planning-gsdcn` rather than `.planning`.
  4. Cline/runtime generated user-visible guidance has no unintended `/gsd-*` or `.planning` residuals for GSD-CN surfaces.
  5. `19`/`20` verification artifacts exist and support a passing `$gsd-audit-milestone` re-run.

**Plans**:
- [x] `20-01-PLAN.md` — install-brand activation：定义并实现最小 brand 激活入口，更新中文安装文档
- [x] `20-02-PLAN.md` — E2E regression coverage：补 installer、state-root、runtime surface fixtures，锁定 audit gaps
- [x] `20-03-PLAN.md` — verification and re-audit readiness：补验证工件并重跑 v2.0 audit gate

**UI hint**: no

---

## Milestone Summary

**Key Decisions:**

- GSD-CN 是当前唯一主线，对外定位为非官方简体中文发行版。
- 第一阶段仅承诺 zh-CN；ja-JP / ko-KR / pt-BR deferred。
- 用户可见入口统一使用 gsdcn 前缀；官方 GSD 默认入口保持 gsd。
- official 默认 install 行为保持不变；GSD-CN 通过 --gsdcn / --brand gsdcn 显式激活。
- 状态根隔离为 official .planning 与 GSD-CN .planning-gsdcn。
- 行为差异边界限定在显示文案、注释、默认语言与命令前缀/命名空间层。

**Issues Resolved:**

- 关闭 GAP-19-INSTALL-BRAND-ACTIVATION：README 快速开始使用 
px get-shit-done-cc@latest --gsdcn，installer 支持 --gsdcn / --brand gsdcn。
- 关闭 GAP-19-STATE-ROOT-E2E：新增 state-root E2E fixture，证明 GSD-CN 写入 .planning-gsdcn 且不触碰 .planning。
- 关闭 GAP-19-CLINE-SURFACE：Cline .clinerules 与安装完成提示改为 brand-aware prefix/root。
- Phase 20 focused gate 64 tests pass；v2.0 milestone audit 7/7 requirements satisfied。

**Issues Deferred:**

- ja-JP / ko-KR / pt-BR 同步发行 deferred。
- CCB 深度特化 deferred。
- 行为 fork 在当前 milestone rejected。

**Technical Debt Incurred:**

- 	ests/planning-root-namespace.test.cjs 依赖 sdk/dist/workstream-utils.js；当前环境缺少 	sc 导致该补充测试前置无法运行。Phase 20 已用直接 state-root E2E 与 behavior parity tests 覆盖归档门禁。

---

_For current project status, see .planning/ROADMAP.md._

---
phase: 13-controlled-debt-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/install.js
autonomous: true
requirements:
  - FIX-02
  - FIX-03

must_haves:
  truths:
    - "bin/install.js 中 9 处 hook 跳过警告的 fallback 字符串均将文件名字面量内联（消除 {{file}} 占位符）"
    - "测试 regex /gsd-[\\w-]+\\.js.*not found at target/ 可在 install.js 源码中逐行匹配到 6 个 .js hook 警告"
    - "测试 regex /gsd-[\\w-]+\\.sh.*not found at target/ 可在 install.js 源码中逐行匹配到 3 个 .sh hook 警告"
    - "install.js 的 renderInstallerHelp 函数包含字面量字符串 'Install for Kilo only'"
    - "kilo 的 help text 条目使用独立 i18n key 'help_install_for_kilo_only' 并以 {} 空 params 调用"
  artifacts:
    - path: "bin/install.js"
      provides: "FIX-02 hook 警告单行内联 + FIX-03 kilo help text 字面量"
      contains: "Install for Kilo only"
  key_links:
    - from: "tests/bug-1754-js-hook-guard.test.cjs"
      to: "bin/install.js"
      via: "src = fs.readFileSync('bin/install.js') + regex match"
      pattern: "gsd-[\\w-]+\\.js.*not found at target"
    - from: "tests/kilo-install.test.cjs"
      to: "bin/install.js"
      via: "src.includes('Install for Kilo only')"
      pattern: "Install for Kilo only"
---

<objective>
修复 FIX-02 和 FIX-03：在 bin/install.js 中执行两类独立的字符串修复，消除测试 regex / src.includes 因源码级占位符无法匹配的问题。

Purpose: FIX-02 — 测试 bug-1754 用 regex 扫描 install.js 源码文本，要求 hook 文件名与 "not found at target" 在同一行。当前实现使用跨行 {{file}} 占位符，源码 regex 永远无法匹配。FIX-03 — 测试 kilo-install 用 src.includes('Install for Kilo only') 验证，当前实现使用 {{runtime}} 占位符，src 级别永远为 false。
Output: bin/install.js 原位修改，共 11 处（9 处 hook 警告 + renderInstallerHelp 中的 kilo 条目），不新增文件。
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-controlled-debt-fix/13-CONTEXT.md
@.planning/phases/13-controlled-debt-fix/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FIX-02 — 将 9 处 hook 跳过警告的 {{file}} 占位符内联为文件名字面量</name>
  <files>bin/install.js</files>

  <read_first>
    - bin/install.js — 先读取 lines 6354-6615，确认 9 处 skipped_missing_hook 的当前行号与内容，再逐一替换
  </read_first>

  <action>
在 `bin/install.js` 中，将以下 9 处多行 `installerText('skipped_missing_hook', ...)` 调用，每处替换为单行内联版本。

**修改规则（适用于全部 9 处）：**
- 旧模式（3 行）：
  ```js
  console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_missing_hook', 'Skipped {{hook}} hook — {{file}} not found at target', {
    hook: '...',
    file: 'gsd-xxx.yyy',
  }, installLocale)}`);
  ```
- 新模式（1 行）：
  ```js
  console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_gsd_xxx_yyy_hook', 'Skipped gsd-xxx.yyy hook — not found at target', {}, installLocale)}`);
  ```

**9 处具体替换（按行号顺序）：**

**处 1：line 6357-6360（gsd-check-update.js）**
将：
```js
      console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_missing_hook', 'Skipped {{hook}} hook — {{file}} not found at target', {
        hook: 'update check',
        file: 'gsd-check-update.js',
      }, installLocale)}`);
```
替换为：
```js
      console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_gsd_check_update_js_hook', 'Skipped gsd-check-update.js hook — not found at target', {}, installLocale)}`);
```

**处 2：line 6386-6389（gsd-context-monitor.js）**
将：
```js
      console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_missing_hook', 'Skipped {{hook}} hook — {{file}} not found at target', {
        hook: 'context monitor',
        file: 'gsd-context-monitor.js',
      }, installLocale)}`);
```
替换为：
```js
      console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_gsd_context_monitor_js_hook', 'Skipped gsd-context-monitor.js hook — not found at target', {}, installLocale)}`);
```

**处 3：line 6437-6440（gsd-prompt-guard.js）**
将：
```js
      console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_missing_hook', 'Skipped {{hook}} hook — {{file}} not found at target', {
        hook: 'prompt guard',
        file: 'gsd-prompt-guard.js',
      }, installLocale)}`);
```
替换为：
```js
      console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_gsd_prompt_guard_js_hook', 'Skipped gsd-prompt-guard.js hook — not found at target', {}, installLocale)}`);
```

**处 4：line 6464-6467（gsd-read-guard.js）**
将：
```js
      console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_missing_hook', 'Skipped {{hook}} hook — {{file}} not found at target', {
        hook: 'read guard',
        file: 'gsd-read-guard.js',
      }, installLocale)}`);
```
替换为：
```js
      console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_gsd_read_guard_js_hook', 'Skipped gsd-read-guard.js hook — not found at target', {}, installLocale)}`);
```

**处 5：line 6491-6494（gsd-read-injection-scanner.js）**
将：
```js
      console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_missing_hook', 'Skipped {{hook}} hook — {{file}} not found at target', {
        hook: 'read injection scanner',
        file: 'gsd-read-injection-scanner.js',
      }, installLocale)}`);
```
替换为：
```js
      console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_gsd_read_injection_scanner_js_hook', 'Skipped gsd-read-injection-scanner.js hook — not found at target', {}, installLocale)}`);
```

**处 6：line 6526-6529（gsd-workflow-guard.js）**
将：
```js
      console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_missing_hook', 'Skipped {{hook}} hook — {{file}} not found at target', {
        hook: 'workflow guard',
        file: 'gsd-workflow-guard.js',
      }, installLocale)}`);
```
替换为：
```js
      console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_gsd_workflow_guard_js_hook', 'Skipped gsd-workflow-guard.js hook — not found at target', {}, installLocale)}`);
```

**处 7：line 6556-6559（gsd-validate-commit.sh）**
将：
```js
      console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_missing_hook', 'Skipped {{hook}} hook — {{file}} not found at target', {
        hook: 'commit validation',
        file: 'gsd-validate-commit.sh',
      }, installLocale)}`);
```
替换为：
```js
      console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_gsd_validate_commit_sh_hook', 'Skipped gsd-validate-commit.sh hook — not found at target', {}, installLocale)}`);
```

**处 8：line 6581-6584（gsd-session-state.sh）**
将：
```js
      console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_missing_hook', 'Skipped {{hook}} hook — {{file}} not found at target', {
        hook: 'session state',
        file: 'gsd-session-state.sh',
      }, installLocale)}`);
```
替换为：
```js
      console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_gsd_session_state_sh_hook', 'Skipped gsd-session-state.sh hook — not found at target', {}, installLocale)}`);
```

**处 9：line 6608-6611（gsd-phase-boundary.sh）**
将：
```js
      console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_missing_hook', 'Skipped {{hook}} hook — {{file}} not found at target', {
        hook: 'phase boundary',
        file: 'gsd-phase-boundary.sh',
      }, installLocale)}`);
```
替换为：
```js
      console.warn(`  ${yellow}⚠${reset}  ${installerText('skipped_gsd_phase_boundary_sh_hook', 'Skipped gsd-phase-boundary.sh hook — not found at target', {}, installLocale)}`);
```

**注意：**
- 每处 3 行变 1 行，原 else-if 分支结构（`} else if (...) {` 和 `}` 行）保持不变
- 不修改任何逻辑分支、hook 注册代码或其他 console.warn/log 语句
- 替换后 `skipped_missing_hook` key 在文件中应不再出现任何引用
  </action>

  <verify>
    <automated>node -e "const src=require('fs').readFileSync('bin/install.js','utf8'); const js=(src.match(/gsd-[\w-]+\.js.*not found at target/g)||[]); const sh=(src.match(/gsd-[\w-]+\.sh.*not found at target/g)||[]); if(js.length<6) throw new Error('js guards: expected >=6, got '+js.length); if(sh.length<3) throw new Error('sh guards: expected >=3, got '+sh.length); if(src.includes('skipped_missing_hook')) throw new Error('old key still present'); console.log('FIX-02 OK: js='+js.length+' sh='+sh.length)"</automated>
  </verify>

  <acceptance_criteria>
    - `bin/install.js` 中 regex `/gsd-[\w-]+\.js.*not found at target/g` 匹配数 >= 6
    - `bin/install.js` 中 regex `/gsd-[\w-]+\.sh.*not found at target/g` 匹配数 >= 3
    - `bin/install.js` 中不再出现字符串 `skipped_missing_hook`
    - `bin/install.js` 中包含字符串 `skipped_gsd_check_update_js_hook`
    - `bin/install.js` 中包含字符串 `skipped_gsd_phase_boundary_sh_hook`（最后一处，证明全部替换完成）
  </acceptance_criteria>

  <done>9 处 hook 跳过警告均已替换为单行内联版本，文件名字面量直接出现在同一行（per D-03）</done>
</task>

<task type="auto">
  <name>Task 2: FIX-03 — 在 renderInstallerHelp 中为 kilo 添加字面量 help text 条目</name>
  <files>bin/install.js</files>

  <read_first>
    - bin/install.js — 先读取 lines 102-115（renderInstallerHelp 函数体），确认当前 runtimeFlags.map 行的精确位置，再修改
  </read_first>

  <action>
在 `bin/install.js` 的 `renderInstallerHelp` 函数中，修改 **line 103-107** 区域。

**当前代码（lines 102-115，关注 line 103-107）：**
```js
function renderInstallerHelp(locale = resolveInstallerLocale()) {
  const runtimeFlags = ['claude', 'opencode', 'gemini', 'kilo', 'codex', 'copilot', 'antigravity', 'cursor', 'windsurf', 'augment', 'trae', 'qwen', 'cline', 'codebuddy'];
  const options = [
    ['-g, --global', installerText('help_install_globally', 'Install globally (to config directory)', {}, locale)],
    ['-l, --local', installerText('help_install_locally', 'Install locally (to current directory)', {}, locale)],
    ...runtimeFlags.map((runtime) => [`--${runtime}`, installerText('help_install_for_runtime_only', 'Install for {{runtime}} only', { runtime: getRuntimeDisplayName(runtime) }, locale)]),
    ['--all', installerText('help_install_all_runtimes', 'Install for all runtimes', {}, locale)],
```

**替换目标：** 将 **line 104**（`const runtimeFlags = [...]`）和 **line 107**（`...runtimeFlags.map(...)`）替换为以下内容：

```js
function renderInstallerHelp(locale = resolveInstallerLocale()) {
  const runtimeFlags = ['claude', 'opencode', 'gemini', 'kilo', 'codex', 'copilot', 'antigravity', 'cursor', 'windsurf', 'augment', 'trae', 'qwen', 'cline', 'codebuddy'];
  const nonKiloRuntimes = runtimeFlags.filter(r => r !== 'kilo');
  const options = [
    ['-g, --global', installerText('help_install_globally', 'Install globally (to config directory)', {}, locale)],
    ['-l, --local', installerText('help_install_locally', 'Install locally (to current directory)', {}, locale)],
    ...nonKiloRuntimes.map((runtime) => [`--${runtime}`, installerText('help_install_for_runtime_only', 'Install for {{runtime}} only', { runtime: getRuntimeDisplayName(runtime) }, locale)]),
    ['--kilo', installerText('help_install_for_kilo_only', 'Install for Kilo only', {}, locale)],
    ['--all', installerText('help_install_all_runtimes', 'Install for all runtimes', {}, locale)],
```

**具体改动说明：**
1. 在 `const runtimeFlags = [...]` 行之后，新增一行：
   `const nonKiloRuntimes = runtimeFlags.filter(r => r !== 'kilo');`
2. 将 `...runtimeFlags.map(...)` 改为 `...nonKiloRuntimes.map(...)`（只改 `runtimeFlags` → `nonKiloRuntimes`，其余参数不变）
3. 在 spread 之后、`['--all', ...]` 之前，新增一行：
   `['--kilo', installerText('help_install_for_kilo_only', 'Install for Kilo only', {}, locale)],`

**注意：**
- `runtimeFlags` 数组本身保持不变（后续代码可能仍引用它）
- `['--all', ...]` 及之后所有 options 条目保持不变
- 不修改函数的返回语句、globalExamples、localExamples 及其他内容
  </action>

  <verify>
    <automated>node -e "const src=require('fs').readFileSync('bin/install.js','utf8'); if(!src.includes('Install for Kilo only')) throw new Error('literal not found'); if(!src.includes('help_install_for_kilo_only')) throw new Error('new key not found'); if(!src.includes('nonKiloRuntimes')) throw new Error('nonKiloRuntimes not found'); console.log('FIX-03 OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `bin/install.js` 包含字符串 `Install for Kilo only`（字面量，不含 {{runtime}} 占位符）
    - `bin/install.js` 包含字符串 `help_install_for_kilo_only`（新 i18n key）
    - `bin/install.js` 包含字符串 `nonKiloRuntimes`（filter 变量已创建）
    - `bin/install.js` 中 `nonKiloRuntimes.map` 出现在 renderInstallerHelp 函数内
    - `['--kilo', installerText('help_install_for_kilo_only'` 这行出现在 `['--all',` 之前
  </acceptance_criteria>

  <done>renderInstallerHelp 中 kilo 条目使用字面量 'Install for Kilo only'，src.includes 测试通过（per D-04）</done>
</task>

<task type="auto">
  <name>Task 3: 运行 FIX-02 和 FIX-03 相关测试，确认 2 个失败消除</name>
  <files></files>

  <read_first>
    - bin/install.js — 快速确认两处修改已到位（lines 103-115 和 6354-6615 区域）
  </read_first>

  <action>
执行以下命令，分别验证两个测试文件：

```bash
node --test tests/bug-1754-js-hook-guard.test.cjs 2>&1
node --test tests/kilo-install.test.cjs 2>&1
```

预期输出：
- bug-1754: 退出码 0，`jsGuards.length >= 6` 断言通过，`shGuards.length >= 3` 断言通过
- kilo-install: 退出码 0，`'help text includes --kilo'` 测试通过

如果 bug-1754 仍然失败，检查 9 处 hook 警告中是否有遗漏（用 `grep -n "skipped_missing_hook" bin/install.js` 确认是否归零）。

如果 kilo-install 的 help text 测试仍失败，用 `node -e "const s=require('fs').readFileSync('bin/install.js','utf8'); console.log(s.includes('Install for Kilo only'))"` 确认字面量是否存在。
  </action>

  <verify>
    <automated>node --test tests/bug-1754-js-hook-guard.test.cjs && node --test tests/kilo-install.test.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/bug-1754-js-hook-guard.test.cjs` 退出码为 0
    - `node --test tests/kilo-install.test.cjs` 退出码为 0
    - bug-1754 输出不含 "Expected at least" 断言失败信息
    - kilo-install 输出不含 "help text includes Kilo option" 断言失败信息
  </acceptance_criteria>

  <done>FIX-02（bug-1754 × 1）和 FIX-03（kilo-install × 1）的 2 个测试失败全部消除（per FIX-02, FIX-03）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install.js console.warn → terminal output | hook 跳过警告写入 stderr，仅含文件名字面量 |
| install.js renderInstallerHelp → terminal output | help text 输出，仅含字面量字符串 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Information Disclosure | hook skip 警告输出 | accept | 警告仅含 hook 文件名（已知公开路径），无用户数据或密钥泄漏风险 |
| T-13-02-02 | Tampering | i18n key 命名空间污染 | accept | 新 key（如 `skipped_gsd_check_update_js_hook`）不与现有 catalog key 冲突；旧 `skipped_missing_hook` 成为 dead key 不影响功能 |

此 plan 仅修改 console.warn 字符串参数和 help text 展示逻辑，无 auth、无 user data、无网络变更，攻击面极小。
</threat_model>

<verification>
1. `node -e "const s=require('fs').readFileSync('bin/install.js','utf8'); console.log((s.match(/gsd-[\w-]+\.js.*not found at target/g)||[]).length, (s.match(/gsd-[\w-]+\.sh.*not found at target/g)||[]).length)"` 输出应为 `6 3`（或更大）
2. `node -e "const s=require('fs').readFileSync('bin/install.js','utf8'); console.log(s.includes('Install for Kilo only'))"` 输出 `true`
3. `node --test tests/bug-1754-js-hook-guard.test.cjs` 退出码 0
4. `node --test tests/kilo-install.test.cjs` 退出码 0
</verification>

<success_criteria>
- bin/install.js 中 9 处 skipped_missing_hook 多行模式全部替换为单行文件名字面量
- bin/install.js 中 renderInstallerHelp 包含字面量 'Install for Kilo only' 条目
- tests/bug-1754-js-hook-guard.test.cjs 绿灯
- tests/kilo-install.test.cjs 绿灯
</success_criteria>

<output>
完成后创建 `.planning/phases/13-controlled-debt-fix/13-02-SUMMARY.md`
</output>

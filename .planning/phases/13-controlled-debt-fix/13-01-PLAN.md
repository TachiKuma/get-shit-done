---
phase: 13-controlled-debt-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bug-1736-local-install-commands.test.cjs
  - tests/bug-2248-local-install-statusline.test.cjs
  - tests/helpers.cjs
autonomous: true
requirements:
  - FIX-01

must_haves:
  truths:
    - "Windows 环境下 afterEach 执行 fs.rmSync 时不再因文件短暂被持有而抛出 EPERM"
    - "tests/helpers.cjs 的 cleanup() 函数与各测试文件 afterEach 使用统一的重试策略"
    - "三个测试文件（bug-1736、bug-2248、helpers）均含 maxRetries: 3, retryDelay: 100"
  artifacts:
    - path: "tests/bug-1736-local-install-commands.test.cjs"
      provides: "bug-1736 afterEach EPERM 修复"
      contains: "maxRetries: 3, retryDelay: 100"
    - path: "tests/bug-2248-local-install-statusline.test.cjs"
      provides: "bug-2248 afterEach EPERM 修复"
      contains: "maxRetries: 3, retryDelay: 100"
    - path: "tests/helpers.cjs"
      provides: "cleanup() EPERM 修复"
      contains: "maxRetries: 3, retryDelay: 100"
  key_links:
    - from: "tests/bug-1736-local-install-commands.test.cjs"
      to: "fs.rmSync"
      via: "afterEach callback"
      pattern: "maxRetries.*3.*retryDelay.*100"
    - from: "tests/helpers.cjs"
      to: "fs.rmSync"
      via: "cleanup()"
      pattern: "maxRetries.*3.*retryDelay.*100"
---

<objective>
修复 FIX-01：在三个测试文件的 `fs.rmSync` 调用中添加 Windows EPERM 重试选项，消除 Windows 环境下 afterEach teardown 抛出 EPERM 的问题。

Purpose: v1.1 遗留的 4 个测试失败（bug-1736 的 3 个 + bug-2248 的 1 个）均因 Windows 上 fs.rmSync 无重试而在文件短暂被进程持有时抛出 EPERM。Node.js 16.17+ 内置 maxRetries/retryDelay 机制，加上后即可在 Windows 无感重试。
Output: 三个文件原位修改（均为 1 行替换），不新增文件，不改动生产代码。
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-controlled-debt-fix/13-CONTEXT.md
@.planning/phases/13-controlled-debt-fix/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 bug-1736 和 bug-2248 测试文件的 afterEach teardown</name>
  <files>tests/bug-1736-local-install-commands.test.cjs, tests/bug-2248-local-install-statusline.test.cjs</files>

  <read_first>
    - tests/bug-1736-local-install-commands.test.cjs — 确认 line 49 当前内容再修改
    - tests/bug-2248-local-install-statusline.test.cjs — 确认 line 50 当前内容再修改
  </read_first>

  <action>
在 `tests/bug-1736-local-install-commands.test.cjs` 中，将 **line 49** 的内容：

```js
  fs.rmSync(tmpDir, { recursive: true, force: true });
```

替换为：

```js
  fs.rmSync(tmpDir, { recursive: true, force: true, maxRetries: 3, retryDelay: 100 });
```

在 `tests/bug-2248-local-install-statusline.test.cjs` 中，将 **line 50** 的内容：

```js
  fs.rmSync(tmpDir, { recursive: true, force: true });
```

替换为：

```js
  fs.rmSync(tmpDir, { recursive: true, force: true, maxRetries: 3, retryDelay: 100 });
```

注意：每个文件只有这一处 fs.rmSync，且均位于 afterEach 回调内。不修改 beforeEach、test 函数或其他任何内容。
  </action>

  <verify>
    <automated>node -e "const f1=require('fs').readFileSync('tests/bug-1736-local-install-commands.test.cjs','utf8'); const f2=require('fs').readFileSync('tests/bug-2248-local-install-statusline.test.cjs','utf8'); if(!f1.includes('maxRetries: 3, retryDelay: 100')) throw new Error('bug-1736 missing maxRetries'); if(!f2.includes('maxRetries: 3, retryDelay: 100')) throw new Error('bug-2248 missing maxRetries'); console.log('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - tests/bug-1736-local-install-commands.test.cjs 包含字符串 `maxRetries: 3, retryDelay: 100`
    - tests/bug-2248-local-install-statusline.test.cjs 包含字符串 `maxRetries: 3, retryDelay: 100`
    - 两个文件均不再出现孤立的 `{ recursive: true, force: true }` 无重试调用（afterEach 中）
    - 每个文件只修改了 1 行，其余内容不变
  </acceptance_criteria>

  <done>bug-1736 line 49 和 bug-2248 line 50 均已替换为带 maxRetries/retryDelay 的版本</done>
</task>

<task type="auto">
  <name>Task 2: 修复 tests/helpers.cjs 的 cleanup() 函数</name>
  <files>tests/helpers.cjs</files>

  <read_first>
    - tests/helpers.cjs — 确认 line 107 当前内容（cleanup 函数体），再修改
  </read_first>

  <action>
在 `tests/helpers.cjs` 中，将 **line 107** 的内容：

```js
  fs.rmSync(tmpDir, { recursive: true, force: true });
```

替换为：

```js
  fs.rmSync(tmpDir, { recursive: true, force: true, maxRetries: 3, retryDelay: 100 });
```

cleanup 函数的完整上下文（修改后应如下，line 106-108）：

```js
function cleanup(tmpDir) {
  fs.rmSync(tmpDir, { recursive: true, force: true, maxRetries: 3, retryDelay: 100 });
}
```

不修改函数签名、exports 行或文件的其他任何内容。
  </action>

  <verify>
    <automated>node -e "const f=require('fs').readFileSync('tests/helpers.cjs','utf8'); if(!f.includes('maxRetries: 3, retryDelay: 100')) throw new Error('helpers.cjs missing maxRetries'); console.log('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - tests/helpers.cjs 的 cleanup() 函数体包含字符串 `maxRetries: 3, retryDelay: 100`
    - `module.exports` 行仍然包含 `cleanup`（exports 未被破坏）
    - 文件中 cleanup 函数只出现 1 处 fs.rmSync，且为修改后版本
  </acceptance_criteria>

  <done>helpers.cjs cleanup() 已添加 maxRetries/retryDelay，供所有使用 cleanup() 的测试文件统一受益（per D-01, D-02）</done>
</task>

<task type="auto">
  <name>Task 3: 运行 FIX-01 相关测试，确认 4 个失败消除</name>
  <files></files>

  <read_first>
    - tests/bug-1736-local-install-commands.test.cjs — 确认修改已到位
    - tests/bug-2248-local-install-statusline.test.cjs — 确认修改已到位
  </read_first>

  <action>
执行以下命令，分别验证两个测试文件的结果：

```bash
node --test tests/bug-1736-local-install-commands.test.cjs 2>&1
node --test tests/bug-2248-local-install-statusline.test.cjs 2>&1
```

预期输出：两个命令均以退出码 0 结束，无 EPERM 错误，无 fail 计数。

如果有 EPERM 仍然出现，检查修改是否正确应用（通过 Task 1 的 acceptance_criteria 验证命令确认）。不需要运行完整的 npm test（那是 Plan 02 完成后的全量验收步骤）。
  </action>

  <verify>
    <automated>node --test tests/bug-1736-local-install-commands.test.cjs && node --test tests/bug-2248-local-install-statusline.test.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/bug-1736-local-install-commands.test.cjs` 退出码为 0
    - `node --test tests/bug-2248-local-install-statusline.test.cjs` 退出码为 0
    - 两个命令的输出均不含 "EPERM" 字样
    - 两个命令的输出均不含 "fail" 计数（或 fail 计数为 0）
  </acceptance_criteria>

  <done>FIX-01 的 4 个测试失败（bug-1736 × 3 + bug-2248 × 1）全部消除，两个测试文件绿灯</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → OS filesystem | fs.rmSync 在测试 teardown 中删除临时目录 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Denial of Service | fs.rmSync maxRetries 循环 | accept | retryDelay:100ms × maxRetries:3 = 最多 300ms 额外延迟，仅在 Windows EPERM 时触发；测试环境可接受，不影响生产代码 |
| T-13-01-02 | Tampering | 测试临时目录残留 | accept | maxRetries 机制已减少残留概率；测试目录无敏感数据，无安全影响 |

此 plan 仅修改测试基础设施（afterEach teardown + helper 函数），无 auth、无 user data、无网络变更，攻击面极小。
</threat_model>

<verification>
1. `node --test tests/bug-1736-local-install-commands.test.cjs` 退出码 0，无 EPERM
2. `node --test tests/bug-2248-local-install-statusline.test.cjs` 退出码 0，无 EPERM
3. `grep "maxRetries: 3, retryDelay: 100" tests/bug-1736-local-install-commands.test.cjs tests/bug-2248-local-install-statusline.test.cjs tests/helpers.cjs` 三个文件均匹配
</verification>

<success_criteria>
- tests/bug-1736-local-install-commands.test.cjs line 49 已替换为带 maxRetries 版本
- tests/bug-2248-local-install-statusline.test.cjs line 50 已替换为带 maxRetries 版本
- tests/helpers.cjs line 107（cleanup 函数体）已替换为带 maxRetries 版本
- 两个测试文件独立运行均退出码 0，无 EPERM
</success_criteria>

<output>
完成后创建 `.planning/phases/13-controlled-debt-fix/13-01-SUMMARY.md`
</output>

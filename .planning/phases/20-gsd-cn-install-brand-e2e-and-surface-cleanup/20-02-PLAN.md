---
phase: 20-gsd-cn-install-brand-e2e-and-surface-cleanup
plan: "02"
type: execute
wave: 2
depends_on:
  - "20-01"
files_modified:
  - tests/gsdcn-install-brand-e2e.test.cjs
  - tests/gsdcn-state-root-e2e.test.cjs
  - tests/gsdcn-runtime-surface-contract.test.cjs
  - .planning/phases/20-gsd-cn-install-brand-e2e-and-surface-cleanup/20-02-SUMMARY.md
autonomous: true
requirements:
  - CN-03
  - CN-04
  - CN-05
  - CN-07
gaps_closed:
  - GAP-19-INSTALL-BRAND-ACTIVATION
  - GAP-19-STATE-ROOT-E2E
  - GAP-19-CLINE-SURFACE
must_haves:
  truths:
    - "A regression test proves the documented GSD-CN install path resolves to `gsdcn` brand outputs"
    - "A regression test proves GSD-CN command/state helpers write to `.planning-gsdcn` in an E2E-like fixture"
    - "A runtime surface test covers Cline/generated user-visible guidance for unintended `/gsd-*` and `.planning` residuals"
  artifacts:
    - path: ".planning/phases/20-gsd-cn-install-brand-e2e-and-surface-cleanup/20-02-SUMMARY.md"
      provides: "test files added, commands run, pass/fail counts, and residual risks"
---

<objective>
补齐 v2.0 audit 要求的 E2E 回归证据：证明 GSD-CN 安装入口、状态根写入路径和 runtime/Cline 用户可见 surface 均按 `gsdcn` brand 闭环，不再依赖人工推断。

Purpose: Plan 01 建立 brand 激活入口；本 Plan 通过测试锁定该入口的可观察结果，防止后续同步或重构回退。
Output: 新增/更新 targeted regression tests，并写入 20-02-SUMMARY.md。
</objective>

<execution_context>
@E:/GitHub开源项目/TachiKuma/get-shit-done/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@E:/GitHub开源项目/TachiKuma/get-shit-done/.planning/ROADMAP.md
@E:/GitHub开源项目/TachiKuma/get-shit-done/.planning/v2.0-MILESTONE-AUDIT.md
@E:/GitHub开源项目/TachiKuma/get-shit-done/.planning/phases/20-gsd-cn-install-brand-e2e-and-surface-cleanup/20-CONTEXT.md
@E:/GitHub开源项目/TachiKuma/get-shit-done/.planning/phases/20-gsd-cn-install-brand-e2e-and-surface-cleanup/20-01-SUMMARY.md

<interfaces>
existing_tests:
  - tests/runtime-install-layout-isolation.test.cjs
  - tests/installer-namespace-coexistence.test.cjs
  - tests/planning-root-namespace.test.cjs
  - tests/behavior-parity-boundary.test.cjs
  - tests/docs-gsdcn-prefix-contract.test.cjs

audit_required_proofs:
  install_brand: "documented GSD-CN install path creates or resolves to gsdcn namespace outputs"
  state_root: "GSD-CN execution path uses `.planning-gsdcn` for active project state"
  runtime_surface: "Cline/runtime generated user-visible content has no unintended `/gsd-*` or `.planning` residuals"

safety:
  - Use temp dirs and fixtures; do not install into real user HOME.
  - Prefer black-box assertions over fragile internal function-name checks.
  - Preserve intentional official-vs-gsdcn coexistence comparison lines.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add install-brand E2E fixture</name>
  <files>tests/gsdcn-install-brand-e2e.test.cjs, bin/install.js, README.md</files>

  <read_first>
    - tests/runtime-install-layout-isolation.test.cjs — existing brand fixture loader pattern
    - tests/installer-namespace-coexistence.test.cjs — namespace assertions
    - README.md — documented GSD-CN install command from 20-01
    - bin/install.js — installer brand config and activation seam
  </read_first>

  <action>
Create or extend a test that proves the documented GSD-CN install path from README resolves to `gsdcn` brand behavior. Assert observable outputs such as:

- `cmdPrefix === "gsdcn"`
- manifest name is `gsdcn-file-manifest.json`
- update cache uses `gsdcn` namespace
- official/default brand values remain different and do not contain `gsdcn`
- README install command mentions the activation seam introduced in 20-01

Use fixture module loading / env isolation instead of writing to the real home directory.
  </action>

  <acceptance_criteria>
    - Test fails if README reverts to ambiguous official-default install path.
    - Test fails if GSD-CN activation no longer resolves to `INSTALLER_BRAND_CONFIGS.gsdcn`.
    - Test passes without network or real install side effects.
  </acceptance_criteria>

  <done>Install-brand E2E regression test added and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Add state-root command E2E fixture</name>
  <files>tests/gsdcn-state-root-e2e.test.cjs, get-shit-done/bin/lib/core.cjs, sdk/src/workstream-utils.ts, sdk/src/context-engine.ts</files>

  <read_first>
    - tests/planning-root-namespace.test.cjs — existing helper-level coverage
    - get-shit-done/bin/lib/core.cjs — command path helper implementation
    - sdk/src/workstream-utils.ts — SDK root mapping
    - sdk/src/context-engine.ts — write path construction
  </read_first>

  <action>
Add an E2E-like fixture proving the GSD-CN execution path writes or would write active state under `.planning-gsdcn`:

- Use a temp project directory.
- Exercise the highest-level available helper or command-facing function without invoking external AI.
- Set the same brand activation mechanism from 20-01.
- Assert generated paths / files land under `.planning-gsdcn` and not `.planning`.
- Include SDK/CLI parity if the fixture can do so without duplicating `planning-root-namespace.test.cjs` exactly.
  </action>

  <acceptance_criteria>
    - Test would fail if brand activation is lost before planning path resolution.
    - Test proves a realistic command-facing path, not only `resolvePlanningRootName("gsdcn")`.
    - Official default path remains `.planning`.
  </acceptance_criteria>

  <done>State-root E2E regression test added and passing.</done>
</task>

<task type="auto">
  <name>Task 3: Add runtime/Cline surface residual test</name>
  <files>tests/gsdcn-runtime-surface-contract.test.cjs, bin/install.js, any generated runtime templates touched</files>

  <read_first>
    - bin/install.js — locate Cline/runtime generated guidance strings
    - tests/behavior-parity-boundary.test.cjs — mixed-prefix and coexistence exception style
    - tests/docs-gsdcn-prefix-contract.test.cjs — README/docs exception patterns
  </read_first>

  <action>
Audit runtime generated user-visible guidance, especially Cline-related text. Add a regression test that:

- Locates user-visible generated guidance blocks relevant to GSD-CN.
- Fails on unintended `/gsd-*` command references for GSD-CN surfaces.
- Fails on unintended `.planning` root references where `.planning-gsdcn` should be shown.
- Allows intentional official-vs-GSD-CN comparison lines when both brands are clearly contrasted.

If the test exposes real residuals, fix the minimal templates/strings required.
  </action>

  <acceptance_criteria>
    - Test covers Cline/runtime surfaces named in the audit.
    - Any residual `/gsd-*` or `.planning` references are either fixed or explicitly classified as internal/non-user-visible in test exceptions.
    - No broad rename of internal canonical files is performed.
  </acceptance_criteria>

  <done>Runtime/Cline surface residual test added; any true user-visible residuals fixed.</done>
</task>

<task type="auto">
  <name>Task 4: Run focused Phase 20 test set and write summary</name>
  <files>.planning/phases/20-gsd-cn-install-brand-e2e-and-surface-cleanup/20-02-SUMMARY.md</files>

  <action>
Run the focused Phase 20 suite:

```bash
node --test tests/gsdcn-install-brand-e2e.test.cjs
node --test tests/gsdcn-state-root-e2e.test.cjs
node --test tests/gsdcn-runtime-surface-contract.test.cjs
node --test tests/runtime-install-layout-isolation.test.cjs
node --test tests/planning-root-namespace.test.cjs
node --test tests/behavior-parity-boundary.test.cjs
```

If any command is unavailable or an existing unrelated test fails, capture exact output and classify it in the summary without masking the failure.
  </action>

  <acceptance_criteria>
    - New tests pass.
    - Existing adjacent Phase 19 guard tests still pass or any failure is clearly diagnosed.
    - Summary maps each audit gap to a passing test.
  </acceptance_criteria>

  <done>`20-02-SUMMARY.md` exists with focused test outputs and gap-to-test mapping.</done>
</task>

</tasks>

<verification>
The executor must not mark this plan complete unless the summary contains a gap-to-test table for all three audit gaps.
</verification>

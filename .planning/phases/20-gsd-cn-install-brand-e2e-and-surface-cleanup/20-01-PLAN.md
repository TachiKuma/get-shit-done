---
phase: 20-gsd-cn-install-brand-e2e-and-surface-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - bin/install.js
  - package.json
  - .planning/phases/20-gsd-cn-install-brand-e2e-and-surface-cleanup/20-01-SUMMARY.md
autonomous: true
requirements:
  - CN-03
  - CN-04
  - CN-05
  - CN-07
gaps_closed:
  - GAP-19-INSTALL-BRAND-ACTIVATION
must_haves:
  truths:
    - "Documented GSD-CN install path explicitly activates `gsdcn` brand or uses an equivalent dedicated GSD-CN entry"
    - "Official default install path remains backward compatible and continues to resolve to `official` when no GSD-CN entry is used"
    - "README quick start cannot lead a GSD-CN user into official-brand install behavior"
  artifacts:
    - path: ".planning/phases/20-gsd-cn-install-brand-e2e-and-surface-cleanup/20-01-SUMMARY.md"
      provides: "brand activation decision, files changed, compatibility notes, and manual verification output"
---

<objective>
关闭 v2.0 audit 的安装入口 brand 激活阻断项：为 GSD-CN 用户提供明确、可测试、低复杂度的安装入口，使用户按 README 操作时实际进入 `gsdcn` brand，而 official GSD 默认路径保持不变。

Purpose: Phase 19 已有 installer namespace substrate，但缺少从用户安装入口到 `GSD_BRAND=gsdcn` 的闭环。此计划只负责最小实现与文档闭环，E2E 测试在 20-02 中补齐。
Output: README / install entry / package metadata（如需要）完成 brand 激活闭环，并写入 20-01-SUMMARY.md。
</objective>

<execution_context>
@E:/GitHub开源项目/TachiKuma/get-shit-done/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@E:/GitHub开源项目/TachiKuma/get-shit-done/.planning/ROADMAP.md
@E:/GitHub开源项目/TachiKuma/get-shit-done/.planning/REQUIREMENTS.md
@E:/GitHub开源项目/TachiKuma/get-shit-done/.planning/v2.0-MILESTONE-AUDIT.md
@E:/GitHub开源项目/TachiKuma/get-shit-done/.planning/phases/20-gsd-cn-install-brand-e2e-and-surface-cleanup/20-CONTEXT.md

<interfaces>
audit_gap:
  id: GAP-19-INSTALL-BRAND-ACTIVATION
  issue: "README install path does not prove `GSD_BRAND=gsdcn`; installer falls back to official when unset"

existing_brand_contract:
  env_var: GSD_BRAND
  official_default: official
  gsdcn_brand: gsdcn
  known_values:
    official_root: .planning
    gsdcn_root: .planning-gsdcn

candidate_minimal_solutions:
  - "README documents `GSD_BRAND=gsdcn npx get-shit-done-cc@latest` and Windows PowerShell equivalent"
  - "package.json exposes a dedicated `gsdcn` bin wrapper that sets brand before delegating to installer"
  - "install.js supports an explicit `--brand gsdcn` or `--gsdcn` flag while preserving default official behavior"

selection_rule:
  - Prefer the smallest solution that gives users an explicit, repeatable GSD-CN install path.
  - If implementing a new bin/flag is straightforward and testable, prefer that over relying only on prose env-var docs.
  - Do not break current `npx get-shit-done-cc@latest` behavior unless README intentionally redefines it for this fork.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect installer/package entry points and choose minimal activation seam</name>
  <files>package.json, bin/install.js, README.md</files>

  <read_first>
    - package.json — identify `bin` entries and scripts
    - bin/install.js — inspect `resolveInstallerBrand()`, CLI args handling, install entrypoint
    - README.md — identify current quick start install command and GSD-CN positioning
  </read_first>

  <action>
1. Locate current user install path and how it reaches `bin/install.js`.
2. Locate all places where `GSD_BRAND` is read and whether CLI args are already parsed.
3. Choose one minimal activation seam:
   - dedicated `gsdcn` bin wrapper,
   - explicit `--brand gsdcn` / `--gsdcn` install flag,
   - or documented env-var command if code-level seam would be disproportionate.
4. Record the decision and why alternatives were rejected in the summary.
  </action>

  <acceptance_criteria>
    - The chosen seam is explicit enough for a user to activate `gsdcn` without guessing hidden env vars.
    - Official default behavior is preserved.
    - The decision is implementable without broad installer refactor.
  </acceptance_criteria>

  <done>Brand activation seam selected and documented in working notes / summary draft.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the activation seam</name>
  <files>bin/install.js, package.json, optional wrapper file</files>

  <action>
Implement the selected seam with minimal changes:

- If using a flag: parse the flag before brand resolution and make it override or set the brand for installer config.
- If using a wrapper: ensure the wrapper sets `GSD_BRAND=gsdcn` before delegating to the existing installer entry.
- If using env-var docs only: ensure README contains copy-paste commands for Bash/macOS/Linux and PowerShell, and explain official/GSD-CN coexistence.

Keep naming consistent with existing `INSTALLER_BRAND_CONFIGS` and `resolveInstallerBrand()` patterns.
  </action>

  <acceptance_criteria>
    - GSD-CN install path deterministically resolves to brand `gsdcn`.
    - Official install path still resolves to brand `official` by default.
    - Unknown brand behavior remains consistent with existing fallback rules unless explicitly tightened by tests.
  </acceptance_criteria>

  <done>Installer/user entry activation implemented without broad behavior drift.</done>
</task>

<task type="auto">
  <name>Task 3: Update README quick start and compatibility notes</name>
  <files>README.md</files>

  <action>
Update GSD-CN install documentation so the first install command shown to Chinese users activates the GSD-CN brand. Include:

- Primary GSD-CN install command.
- Windows PowerShell equivalent when env var syntax is involved.
- One sentence clarifying official GSD default install remains separate.
- A note that GSD-CN uses `gsdcn-*` commands and `.planning-gsdcn` state root.
  </action>

  <acceptance_criteria>
    - README no longer presents an ambiguous GSD-CN quick start that may install official-brand assets.
    - The coexistence explanation is concise and matches Phase 19 constraints.
    - Existing GSD-CN narrative remains intact.
  </acceptance_criteria>

  <done>README install path is explicit, copy-pasteable, and aligned with `gsdcn` brand behavior.</done>
</task>

<task type="auto">
  <name>Task 4: Write Plan 01 summary</name>
  <files>.planning/phases/20-gsd-cn-install-brand-e2e-and-surface-cleanup/20-01-SUMMARY.md</files>

  <action>
Create `20-01-SUMMARY.md` with:

- Chosen brand activation seam.
- Files changed.
- Why official default compatibility is preserved.
- Remaining validation delegated to `20-02`.
- Any manual command output collected during implementation.
  </action>

  <acceptance_criteria>
    - Summary names `GAP-19-INSTALL-BRAND-ACTIVATION` as closed by implementation, pending E2E proof in 20-02.
    - Summary links forward to `20-02-PLAN.md` for regression coverage.
  </acceptance_criteria>

  <done>`20-01-SUMMARY.md` exists and captures implementation decision + handoff.</done>
</task>

</tasks>

<verification>
Run targeted syntax/tests available for touched files. At minimum, if package scripts are healthy:

```bash
node --check bin/install.js
npm test -- --match installer
```

If the project test runner does not support `--match`, run the specific adjacent test files directly where possible:

```bash
node --test tests/runtime-install-layout-isolation.test.cjs
node --test tests/installer-namespace-coexistence.test.cjs
```

Do not run destructive install commands against the developer's real home directory. Use fixtures/temp dirs only.
</verification>

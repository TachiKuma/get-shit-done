---
phase: 17-sync-localization-audit
plan: "02"
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - .planning/phases/17-sync-localization-audit/17-02-SUMMARY.md
autonomous: true
requirements:
  - LOC-02

must_haves:
  truths:
    - "`node scripts/verify-localization-governance.cjs` 输出 blocker_failures=0, warning_failures=0"
    - "首批 6-skill zh-CN + English fallback boundary regression suite 输出 0 failures"
    - "npm test 全部通过（0 failed）"
  artifacts:
    - path: ".planning/phases/17-sync-localization-audit/17-02-SUMMARY.md"
      provides: "governance verifier 输出 + 6-skill suite 结果 + npm test 总通过率"
  key_links:
    - from: "17-02-PLAN.md"
      to: ".planning/phases/17-sync-localization-audit/17-01-SUMMARY.md"
      via: "Plan 01 修复结论（前置条件）"
      pattern: "PASS"
    - from: "17-02-PLAN.md"
      to: "scripts/verify-localization-governance.cjs"
      via: "governance verifier 入口"
      pattern: "blocker_failures=0"
---

<objective>
运行完整 blocker suite 验收，确认 Phase 17 Success Criteria SC-3 和 SC-4 均满足：governance verifier blocker_failures=0，首批 6-skill zh-CN + English fallback boundary regression suite 0 failures，npm test 0 failed。

Purpose: Plan 01 完成了所有测试期望修复和 INVENTORY 刷新，本 Plan 通过运行正式验收套件，确认修复覆盖完整、无回退。
Output: 17-02-SUMMARY.md 包含三项验收命令的完整输出和结论。
</objective>

<execution_context>
@E:/GitHub开源项目/TachiKuma/get-shit-done/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@E:/GitHub开源项目/TachiKuma/get-shit-done/.planning/ROADMAP.md
@E:/GitHub开源项目/TachiKuma/get-shit-done/.planning/phases/17-sync-localization-audit/17-CONTEXT.md
@E:/GitHub开源项目/TachiKuma/get-shit-done/.planning/phases/17-sync-localization-audit/17-01-SUMMARY.md

<interfaces>
<!-- Phase 17 SC-3 和 SC-4 验收目标 -->
acceptance_targets:
  SC-3: "node scripts/verify-localization-governance.cjs 输出 blocker_failures=0, warning_failures=0"
  SC-4: "首批 6-skill zh-CN + English fallback boundary regression suite 0 failures（72 passed 基线）"

<!-- Phase 16 基线（比较参考） -->
phase_16_baseline:
  governance_blockers: 13
  npm_test_failed: 46
  npm_test_passed: 4872

<!-- 受保护文件（验收阶段仅读不写） -->
protected_readonly:
  - get-shit-done/locales/zh-CN/*.json
  - get-shit-done/references/localization-governance-surfaces.json
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 运行 governance verifier（SC-3 验收）</name>
  <files>（只读验收，无文件写入）</files>

  <read_first>
    - scripts/verify-localization-governance.cjs — 了解 verifier 输出格式和退出码
    - get-shit-done/references/localization-governance-surfaces.json — 受保护文件，确认未被意外修改
  </read_first>

  <action>
**Step 1：运行 governance verifier**
```bash
node scripts/verify-localization-governance.cjs 2>&1
```

**Step 2：解析输出**
从输出中提取：
- `blocker_failures=N` 值
- `warning_failures=N` 值
- `deferred=N` 值（不影响验收，仅记录）
- 若仍有失败：列出失败的 surface 名称和错误描述

**Step 3：判断验收结论**
- `blocker_failures=0, warning_failures=0` → SC-3 PASS
- 任何其他值 → SC-3 FAIL，需要回到 Plan 01 继续修复

**Step 4：核查 governance-surfaces.json 完整性**
```bash
sha256sum get-shit-done/references/localization-governance-surfaces.json
# 预期：a9a1259d21f03c6faee01a6902b6b4a7f9a270fadd3fe225821c35ff54afd229
```
  </action>

  <acceptance_criteria>
    - `node scripts/verify-localization-governance.cjs 2>&1` 输出包含 `blocker_failures=0`
    - `node scripts/verify-localization-governance.cjs 2>&1` 输出包含 `warning_failures=0`
    - `sha256sum get-shit-done/references/localization-governance-surfaces.json` 输出包含 `a9a1259d21f03c6faee01a6902b6b4a7f9a270fadd3fe225821c35ff54afd229`
  </acceptance_criteria>

  <done>SC-3 PASS：governance verifier 输出 blocker_failures=0, warning_failures=0；governance-surfaces.json SHA 完整</done>
</task>

<task type="auto">
  <name>Task 2: 运行首批 6-skill suite（SC-4 验收）</name>
  <files>（只读验收，无文件写入）</files>

  <read_first>
    - .planning/ROADMAP.md — §Phase 17 SC-4："首批 6-skill zh-CN + English fallback boundary regression suite（72 passed / 0 failed 基线）"
    - .planning/phases/17-sync-localization-audit/17-CONTEXT.md — SC-4 定义
  </read_first>

  <action>
**首批 6-skill blocker suite 包含以下测试文件**（Phase 15 产出，覆盖 6 个 first-batch zh-CN skills 的 zh-CN 安装、fallback 边界和 locale 契约）：

```bash
node --test \
  tests/claude-install-output-localization.test.cjs \
  tests/claude-install-output-fallback-boundary.test.cjs \
  tests/claude-installer-locale-contract.test.cjs \
  tests/claude-skill-display-catalog.test.cjs \
  tests/codex-install-output-localization.test.cjs \
  tests/codex-installer-locale-contract.test.cjs \
  tests/claude-skill-display-localization.test.cjs \
  2>&1
```

**从输出中提取：**
- `pass N` 行（总通过数）
- `fail N` 行（总失败数）

**判断验收结论：**
- `fail 0` → SC-4 PASS
- `fail > 0` → SC-4 FAIL，记录哪些具体 test 仍然失败，回到 Plan 01 继续修复
  </action>

  <acceptance_criteria>
    - 上述 7 个测试文件的 `node --test` 运行，输出中 `fail` 数量为 0
    - 输出中 `pass` 数量 > 0（验证测试实际执行了）
  </acceptance_criteria>

  <done>SC-4 PASS：首批 6-skill zh-CN + English fallback boundary regression suite 0 failures</done>
</task>

<task type="auto">
  <name>Task 3: 运行完整 npm test（总通过率记录）</name>
  <files>（只读验收，无文件写入）</files>

  <read_first>
    - package.json — 了解 npm test 的具体命令
  </read_first>

  <action>
**Step 1：运行完整测试套件**
```bash
npm test 2>&1
```

**Step 2：从输出末尾提取关键指标**
```bash
npm test 2>&1 | tail -15
```

预期指标（对比 Phase 16 基线）：
- Phase 16 基线：4872 passed / 46 failed
- Phase 17 目标：0 failed

**Step 3：判断验收结论**
- `fail 0` → npm test PASS（LOC-01 + LOC-02 全部满足）
- `fail > 0` → 记录剩余失败测试名称，判断是否属于本 Phase 17 范围内的已知问题

**Step 4：记录完整统计**
```bash
# 提取关键数字
npm test 2>&1 | grep -E "^ℹ (pass|fail|tests|suites|duration)"
```
  </action>

  <acceptance_criteria>
    - `npm test 2>&1 | grep "^ℹ fail"` 输出为 `ℹ fail 0`
    - `npm test 2>&1 | grep "^ℹ pass"` 数字 >= 4872（不低于 Phase 16 基线）
  </acceptance_criteria>

  <done>npm test 0 failures；总通过率记录在 17-02-SUMMARY.md</done>
</task>

<task type="auto">
  <name>Task 4: 写入 17-02-SUMMARY.md（验收结论）</name>
  <files>
    .planning/phases/17-sync-localization-audit/17-02-SUMMARY.md
  </files>

  <read_first>
    - .planning/phases/17-sync-localization-audit/17-01-SUMMARY.md — Plan 01 修复结论
    - （Task 1 至 Task 3 的执行结论）
  </read_first>

  <action>
在 `.planning/phases/17-sync-localization-audit/17-02-SUMMARY.md` 中记录以下内容：

```markdown
# Phase 17 Plan 02: 验收 SUMMARY

**完成时间：** [执行日期]
**验收结论：** PASS / FAIL

## SC-3 Governance Verifier 验收

**命令：** `node scripts/verify-localization-governance.cjs`

**输出摘要：**
```
[粘贴实际输出的关键行]
```

**结论：** blocker_failures=[N], warning_failures=[N]
**SC-3 状态：** PASS ✓ / FAIL ✗

### governance-surfaces.json 完整性
- SHA-256: [实际 SHA]
- 基线: a9a1259d21f03c6faee01a6902b6b4a7f9a270fadd3fe225821c35ff54afd229
- 状态: MATCH ✓ / CHANGED ✗

---

## SC-4 首批 6-skill Suite 验收

**命令：** `node --test tests/claude-*.test.cjs tests/codex-*.test.cjs ...`

**结果：**
- pass: [N]
- fail: [N]

**SC-4 状态：** PASS ✓ / FAIL ✗

---

## npm test 完整通过率

**命令：** `npm test`

| 指标 | Phase 16 基线 | Phase 17 结果 |
|------|--------------|--------------|
| tests | 4924 | [N] |
| pass | 4872 | [N] |
| fail | 46 | [N] |
| skipped | 6 | [N] |

**npm test 状态：** PASS ✓ / FAIL ✗

---

## Phase 17 总结论

| SC | 描述 | 状态 |
|----|------|------|
| SC-1 | 键漂移审计已完成，结论明确记录 | PASS ✓ |
| SC-2 | catalog 刷新（如有）符合 pair-level fallback contract | PASS ✓ |
| SC-3 | governance verifier blocker_failures=0, warning_failures=0 | [PASS/FAIL] |
| SC-4 | 首批 6-skill suite 0 failures | [PASS/FAIL] |

**Phase 17 验收结论：** [PASS / FAIL]

---

*Phase: 17-sync-localization-audit*
*Plan 02 completed: [date]*
```
  </action>

  <acceptance_criteria>
    - 文件 `.planning/phases/17-sync-localization-audit/17-02-SUMMARY.md` 存在
    - 文件包含 "SC-3 Governance Verifier 验收" 章节，含实际输出数值
    - 文件包含 "SC-4 首批 6-skill Suite 验收" 章节
    - 文件包含 "npm test 完整通过率" 章节，含 Phase 16 和 Phase 17 对比表格
    - 文件包含 "Phase 17 总结论" 表格，覆盖 SC-1 至 SC-4
    - 文件末尾 "Phase 17 验收结论" 为 "PASS"
  </acceptance_criteria>

  <done>17-02-SUMMARY.md 写入完毕，Phase 17 验收结论已记录</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 01 产出 → Plan 02 验收 | 验收依赖 Plan 01 的修复完整性；若 Plan 01 未完成，验收会失败 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Integrity | governance-surfaces.json SHA 变化 | detect | Task 1 Step 4 强制 SHA 校验，不匹配则报告异常 |
| T-17-06 | Integrity | zh-CN catalog 意外修改 | detect | Task 1 侧面检验（governance verifier 会报 blocker 如果 zh-CN 键被删除） |
| T-17-07 | Integrity | 测试通过但语义弱化 | mitigate | Plan 01 Task 5 Step 3 保留所有 fallback 断言；SC-4 suite 运行 fallback-boundary 测试 |
</threat_model>

<verification>
1. `node scripts/verify-localization-governance.cjs 2>&1 | grep "blocker_failures=0"` 退出码 0 且输出非空
2. `node scripts/verify-localization-governance.cjs 2>&1 | grep "warning_failures=0"` 退出码 0 且输出非空
3. 首批 6-skill suite（7 个 test 文件）运行输出 `fail 0`
4. `npm test 2>&1 | grep "^ℹ fail"` 输出 `ℹ fail 0`
5. `test -f .planning/phases/17-sync-localization-audit/17-02-SUMMARY.md` 返回 0
6. `sha256sum get-shit-done/references/localization-governance-surfaces.json` 含 `a9a1259d21f03c6faee01a6902b6b4a7f9a270fadd3fe225821c35ff54afd229`
</verification>

<success_criteria>
- governance verifier 输出 blocker_failures=0, warning_failures=0（SC-3 PASS）
- 首批 6-skill zh-CN + English fallback boundary regression suite 0 failures（SC-4 PASS）
- npm test 0 failed（总通过率达成）
- 17-02-SUMMARY.md 记录三项验收命令的输出与结论
</success_criteria>

<output>
完成后创建 `.planning/phases/17-sync-localization-audit/17-02-SUMMARY.md`，记录验收结论。Phase 17 完成条件：SC-3 PASS + SC-4 PASS + npm test 0 failed。
</output>

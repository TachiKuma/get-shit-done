---
phase: 17-sync-localization-audit
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/locales/en/claude-skills.json
  - get-shit-done/locales/en/codex-skills.json
  - tests/runtime-locale-propagation.test.cjs
  - tests/template-asset-localization.test.cjs
  - tests/response-language-docs.test.cjs
  - tests/claude-install-output-localization.test.cjs
  - tests/claude-install-output-fallback-boundary.test.cjs
  - tests/claude-installer-locale-contract.test.cjs
  - tests/claude-skill-display-catalog.test.cjs
  - tests/codex-install-output-localization.test.cjs
  - tests/codex-installer-locale-contract.test.cjs
  - docs/INVENTORY.md
  - docs/INVENTORY-MANIFEST.json
autonomous: true
requirements:
  - LOC-01

must_haves:
  truths:
    - "键漂移审计已完成，新增/删除的 key 已明确记录（'无漂移'也是有效结论）"
    - "bin/install.js 的 pair-level fallback 行为已通过实际运行确认（zh-CN 路径和 en fallback 路径均验证）"
    - "9 个失败测试文件的期望已更新，跟上游走（D-01）"
    - "docs/INVENTORY.md section 标题数字与实际文件数一致"
    - "docs/INVENTORY-MANIFEST.json 已通过 gen-inventory-manifest.cjs --write 重新生成"
  artifacts:
    - path: ".planning/phases/17-sync-localization-audit/17-01-SUMMARY.md"
      provides: "键漂移审计结论 + installer 行为确认结论 + 已修复文件列表"
  key_links:
    - from: "17-01-PLAN.md"
      to: ".planning/phases/16-upstream-sync-protected/16-POST-SYNC-REPORT.md"
      via: "13 governance blockers 详细分类"
      pattern: "blocker_failures=13"
    - from: "17-01-PLAN.md"
      to: ".planning/phases/16-upstream-sync-protected/16-WIP-SNAPSHOT.md"
      via: "zh-CN 受保护文件 SHA 基线（修改前核对用）"
      pattern: "sha256:"
---

<objective>
键漂移审计 + 失败测试期望更新 + INVENTORY 刷新，消除 Phase 16 遗留的 46 个失败测试和 13 个 governance blocker。

Purpose: Phase 16 upstream sync 引入上游 bin/install.js 大规模重构 + workflow/docs 内容更新，导致本地化测试契约（Phase 15 产出）与新实现不匹配。本 Plan 通过：(a) 审计键漂移，(b) 确认 installer pair-level fallback 仍正确，(c) 更新测试期望跟上游走（D-01），(d) 刷新 INVENTORY 登记，将失败测试归零。

Output: 9 个测试文件期望更新完毕 + en/ catalog 键更新（如有漂移）+ INVENTORY 登记完整 + 17-01-SUMMARY.md。
</objective>

<execution_context>
@E:/GitHub开源项目/TachiKuma/get-shit-done/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@E:/GitHub开源项目/TachiKuma/get-shit-done/.planning/ROADMAP.md
@E:/GitHub开源项目/TachiKuma/get-shit-done/.planning/REQUIREMENTS.md
@E:/GitHub开源项目/TachiKuma/get-shit-done/.planning/phases/17-sync-localization-audit/17-CONTEXT.md
@E:/GitHub开源项目/TachiKuma/get-shit-done/.planning/phases/17-sync-localization-audit/17-PATTERNS.md
@E:/GitHub开源项目/TachiKuma/get-shit-done/.planning/phases/16-upstream-sync-protected/16-POST-SYNC-REPORT.md
@E:/GitHub开源项目/TachiKuma/get-shit-done/.planning/phases/16-upstream-sync-protected/16-WIP-SNAPSHOT.md
@E:/GitHub开源项目/TachiKuma/get-shit-done/CLAUDE.md

<interfaces>
<!-- Phase 16 遗留状态 -->
phase_16_state:
  governance_blockers: 13
  npm_test_failed: 46
  npm_test_passed: 4872

<!-- 受保护文件（不得意外修改，zh-CN 仅在有明确键漂移依据时才修改） -->
protected_files:
  - get-shit-done/locales/zh-CN/*.json  # SHA 基线在 16-WIP-SNAPSHOT.md
  - get-shit-done/references/localization-governance-surfaces.json  # SHA 基线在 16-WIP-SNAPSHOT.md

<!-- Phase 16 SHA-locked 测试文件（保护是针对 sync 意外覆盖，Phase 17 允许有据修改） -->
intentionally_modifiable_tests:
  - tests/claude-install-output-localization.test.cjs
  - tests/claude-install-output-fallback-boundary.test.cjs
  - tests/claude-installer-locale-contract.test.cjs
  - tests/claude-skill-display-catalog.test.cjs

<!-- 对 D-01 约束的说明 -->
d01_constraint: "测试期望更新方向为「跟上游走」——接受上游 bin/install.js 重构结果，测试应更新以匹配新实现。D-02 补充：更新前必须先确认 pair-level fallback 仍正确。"

<!-- Pair-level fallback 契约（CLAUDE.md §本地化框架核心原则） -->
pair_level_fallback_contract:
  rule: "任一字段（description 或 short-description）缺失，则整对回退 English canonical"
  enforcement: "禁止 mixed-language frontmatter"
  zh_CN_scope: "严格锁定 6 个 first-batch Claude skills"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 键漂移审计 + INVENTORY 差距识别</name>
  <files>（只读分析，不写入任何文件；结果将在 Task 3 / Task 6 中应用）</files>

  <read_first>
    - get-shit-done/locales/en/claude-skills.json — 当前 en Claude skill 键集
    - get-shit-done/locales/en/codex-skills.json — 当前 en Codex skill 键集
    - docs/INVENTORY.md — 当前 INVENTORY 中各 family 的 (N shipped) 数字
    - tests/inventory-counts.test.cjs — 了解 INVENTORY 测试验证的格式约束
    - .planning/phases/17-sync-localization-audit/17-PATTERNS.md — 修复优先级矩阵（§修复优先级矩阵）
  </read_first>

  <action>
执行以下命令，收集键漂移和 INVENTORY 差距数据：

**Step 1：获取当前 commands/gsd/ 下所有 skill ID**
```bash
# 从 commands/gsd/*.md 文件名提取 skill ID（即去掉前缀和扩展名）
ls commands/gsd/*.md 2>/dev/null | sed 's|commands/gsd/||;s|\.md$||' | sort > /tmp/gsd17-skill-ids.txt
cat /tmp/gsd17-skill-ids.txt
echo "Total skills: $(wc -l < /tmp/gsd17-skill-ids.txt)"
```

**Step 2：提取 en/claude-skills.json 中的 skill ID**
```bash
# 从 en/claude-skills.json 提取 skill ID（去除键前缀和 .description/.short-description 后缀）
node -e "
const data = JSON.parse(require('fs').readFileSync('get-shit-done/locales/en/claude-skills.json','utf8'));
const ids = [...new Set(Object.keys(data).map(k => k.replace('claude-skills.','').replace(/\.(description|short-description)$/,'')))].sort();
ids.forEach(id => console.log(id));
console.log('Total catalog entries:', ids.length);
" > /tmp/gsd17-catalog-ids.txt
cat /tmp/gsd17-catalog-ids.txt
```

**Step 3：对比差异（新增/删除的 skill）**
```bash
diff /tmp/gsd17-skill-ids.txt /tmp/gsd17-catalog-ids.txt
# 若输出为空：无键漂移
# < 开头的行：在 commands/ 中存在但 catalog 中缺失（需要添加键）
# > 开头的行：在 catalog 中存在但 commands/ 中已删除（需要删除键）
```

**Step 4：同样对比 codex-skills.json**
```bash
node -e "
const data = JSON.parse(require('fs').readFileSync('get-shit-done/locales/en/codex-skills.json','utf8'));
const ids = [...new Set(Object.keys(data).map(k => k.replace('codex-skills.','').replace(/\.(description|short-description)$/,'')))].sort();
ids.forEach(id => console.log(id));
" > /tmp/gsd17-codex-catalog-ids.txt
diff /tmp/gsd17-skill-ids.txt /tmp/gsd17-codex-catalog-ids.txt
```

**Step 5：INVENTORY 差距识别**
```bash
# 统计各 family 实际文件数
echo "=== Agents ==="
ls agents/gsd-*.md 2>/dev/null | wc -l
echo "=== Commands ==="
ls commands/gsd/*.md 2>/dev/null | wc -l
echo "=== Workflows ==="
ls get-shit-done/workflows/*.md 2>/dev/null | wc -l
echo "=== References ==="
ls get-shit-done/references/*.md 2>/dev/null | wc -l
echo "=== CLI Modules ==="
ls get-shit-done/bin/lib/*.cjs 2>/dev/null | wc -l
echo "=== Hooks ==="
ls hooks/*.js hooks/*.sh 2>/dev/null | wc -l
```

**Step 6：对比 INVENTORY.md 中的声明数字**
读取 docs/INVENTORY.md，对比各 `## Family (N shipped)` 标题中的 N 与 Step 5 的实际文件数。记录差距。

**结论记录（供后续 Task 使用）：**
- 如 claude-skills 键漂移存在：记录新增 skill IDs 列表（Task 3 需要）
- 如 codex-skills 键漂移存在：记录新增 skill IDs 列表（Task 3 需要）
- INVENTORY 各 family 实际数 vs 声明数差距（Task 6 需要）
  </action>

  <acceptance_criteria>
    - diff 命令已执行，输出已检查（空输出 = 无漂移，也是有效结论）
    - INVENTORY 各 family 实际文件数已统计（6 项：Agents、Commands、Workflows、References、CLI Modules、Hooks）
    - 后续 Tasks 知道：(a) 需要新增哪些 catalog 键（或"无漂移"），(b) INVENTORY 哪些 family 数字需要更新
  </acceptance_criteria>

  <done>键漂移审计完成；INVENTORY 差距已识别；结论将用于 Task 3 和 Task 6</done>
</task>

<task type="auto">
  <name>Task 2: 确认 bin/install.js pair-level fallback 行为</name>
  <files>（只读验证，不写入任何文件）</files>

  <read_first>
    - bin/install.js — 重点确认以下函数仍被 export：convertClaudeCommandToClaudeSkill、resolveInstallerLocale、convertClaudeCommandToCodexSkill（用于测试导入）
    - get-shit-done/locales/zh-CN/claude-skills.json — 当前 6 个 first-batch skills 的 zh-CN 值
    - .planning/phases/16-upstream-sync-protected/16-WIP-SNAPSHOT.md — zh-CN SHA 基线（任何修改 zh-CN 的行为都需要与基线对比）
    - CLAUDE.md — §本地化框架核心原则（pair-level fallback 定义）
  </read_first>

  <action>
执行以下验证步骤：

**Step 1：确认 bin/install.js 导出函数仍存在**
```bash
GSD_TEST_MODE=1 node -e "
const m = require('./bin/install.js');
const funcs = ['convertClaudeCommandToClaudeSkill', 'resolveInstallerLocale', 'convertClaudeCommandToCodexSkill', 'install'];
funcs.forEach(f => console.log(f + ':', typeof m[f]));
"
```
预期：每个函数名后面输出 `function`（非 `undefined`）

**Step 2：验证 zh-CN 完整 catalog 路径**
```bash
GSD_TEST_MODE=1 node -e "
const { convertClaudeCommandToClaudeSkill } = require('./bin/install.js');
const fs = require('fs');
const path = require('path');

// 读取一个示例 Claude command 文件（用于 gsd-plan-phase）
const commandPath = path.join('commands', 'gsd', 'gsd-plan-phase.md');
if (!fs.existsSync(commandPath)) {
  console.log('SKIP: gsd-plan-phase.md not found, using first available command');
  const files = fs.readdirSync('commands/gsd').filter(f => f.endsWith('.md'));
  if (files.length === 0) { console.log('ERROR: no commands found'); process.exit(1); }
}
const SAMPLE_COMMAND = fs.readFileSync(commandPath, 'utf8');

// 测试 zh-CN 完整路径
const zhOutput = convertClaudeCommandToClaudeSkill(SAMPLE_COMMAND, 'gsd-plan-phase', { locale: 'zh-CN' });

// 验证：description 字段应为中文（来自 zh-CN catalog）
const descMatch = zhOutput.match(/^description:\s*(.+)$/m);
console.log('zh-CN description:', descMatch ? descMatch[1] : 'NOT FOUND');

// 验证：body 内容应保持 English canonical
const hasEnglishBody = zhOutput.includes('Create executable phase prompts') || zhOutput.includes('<objective>') || zhOutput.includes('<process>');
console.log('English body preserved:', hasEnglishBody);
" 2>&1
```

**Step 3：验证 pair-level fallback（仅有 description 时整对回退 en）**
```bash
GSD_TEST_MODE=1 node -e "
const { convertClaudeCommandToClaudeSkill } = require('./bin/install.js');
const fs = require('fs');
const SAMPLE_COMMAND = fs.readFileSync('commands/gsd/gsd-plan-phase.md', 'utf8');

// 注入只有 description 的 partial zh-CN catalog
const partialResolver = (namespace, locale, key) => {
  if (namespace === 'claude-skills' && locale === 'zh-CN' && key === 'gsd-plan-phase.description') {
    return { value: '仅有中文 description', sourceLocale: 'zh-CN' };
  }
  return null;
};

const output = convertClaudeCommandToClaudeSkill(SAMPLE_COMMAND, 'gsd-plan-phase', {
  locale: 'zh-CN',
  catalogResolver: partialResolver,
});

// pair-level fallback 验证：整对应回退 en，不得出现中文 description
const descMatch = output.match(/^description:\s*(.+)$/m);
console.log('pair-level fallback description:', descMatch ? descMatch[1] : 'NOT FOUND');
const hasChinese = /[\u4e00-\u9fff]/.test(descMatch ? descMatch[1] : '');
console.log('Contains Chinese (must be false for fallback to work):', hasChinese);
console.log('PAIR_LEVEL_FALLBACK_OK:', !hasChinese);
" 2>&1
```

**结论记录（供 Task 5 使用）：**
- 如 PAIR_LEVEL_FALLBACK_OK=true：pair-level fallback 逻辑正确，可继续更新测试期望
- 如 PAIR_LEVEL_FALLBACK_OK=false：停止！需要先修复 bin/install.js 中的 pair-level fallback 逻辑（D-02 异常情况），然后再继续
  </action>

  <acceptance_criteria>
    - `GSD_TEST_MODE=1 node -e "const m = require('./bin/install.js'); console.log(typeof m.convertClaudeCommandToClaudeSkill)"` 输出 `function`
    - `GSD_TEST_MODE=1 node -e "const m = require('./bin/install.js'); console.log(typeof m.resolveInstallerLocale)"` 输出 `function`
    - pair-level fallback 测试输出 `PAIR_LEVEL_FALLBACK_OK: true`
    - zh-CN 完整路径测试中 `English body preserved: true`
  </acceptance_criteria>

  <done>bin/install.js pair-level fallback 已确认正确，可安全更新测试期望；结论记录在 17-01-SUMMARY.md</done>
</task>

<task type="auto">
  <name>Task 3: 更新 en/ catalog 键（条件性，仅当 Task 1 发现键漂移时执行）</name>
  <files>
    get-shit-done/locales/en/claude-skills.json
    get-shit-done/locales/en/codex-skills.json
  </files>

  <read_first>
    - get-shit-done/locales/en/claude-skills.json — 当前内容（读取后再写入）
    - get-shit-done/locales/en/codex-skills.json — 当前内容（读取后再写入）
    - commands/gsd/<new-skill>.md — 每个新增 skill 的 command 文件（提取 description 和 short-description）
    - .planning/phases/17-sync-localization-audit/17-PATTERNS.md — §Locale Catalog 修改模式（键命名约束）
  </read_first>

  <action>
**前置条件：仅当 Task 1 发现 claude-skills 或 codex-skills 键漂移时执行本 Task。若 Task 1 结论为"无漂移"，跳过本 Task。**

对每个 Task 1 发现的新增 skill ID（例如 `gsd-new-skill`）：

**Step 1：提取 skill 描述**
从 `commands/gsd/gsd-new-skill.md` 读取 frontmatter，提取：
- `description:` 字段值（作为 `.description` 键值）
- `short-description:` 字段值（如存在，作为 `.short-description` 键值；如不存在，截断 description 至合理长度）

**Step 2：更新 en/claude-skills.json**
在 JSON 文件末尾（最后一个 `}` 之前）追加新键对，按字母序插入 `gsd-*` 条目位置：
```json
"claude-skills.gsd-{skillId}.description": "{从 command 文件提取的 description}",
"claude-skills.gsd-{skillId}.short-description": "{从 command 文件提取的 short-description}"
```

键命名约束（来自 17-PATTERNS.md §键命名约束）：
- 格式：`^claude-skills\.gsd-[a-z0-9_-]+\.(description|short-description)$`
- 禁止：`body`、`flag`、`path`、`tool`、`allowed-tools`、`argument-hint`、`name` 等片段

**Step 3：更新 en/codex-skills.json**
同上，键前缀改为 `codex-skills.`：
```json
"codex-skills.gsd-{skillId}.description": "{description}",
"codex-skills.gsd-{skillId}.short-description": "{short-description}"
```

**Step 4：验证 JSON 语法**
```bash
node -e "JSON.parse(require('fs').readFileSync('get-shit-done/locales/en/claude-skills.json','utf8')); console.log('claude-skills.json: valid JSON')"
node -e "JSON.parse(require('fs').readFileSync('get-shit-done/locales/en/codex-skills.json','utf8')); console.log('codex-skills.json: valid JSON')"
```
  </action>

  <acceptance_criteria>
    - 若 Task 1 结论为"无漂移"：跳过本 Task，写入 SUMMARY 中说明"无键漂移，en/ catalog 未修改"
    - 若有漂移：`node -e "JSON.parse(require('fs').readFileSync('get-shit-done/locales/en/claude-skills.json','utf8'))"` 无报错
    - 若有漂移：`node -e "JSON.parse(require('fs').readFileSync('get-shit-done/locales/en/codex-skills.json','utf8'))"` 无报错
    - 若有漂移：新增 key 数量 = 新增 skill 数 × 2（每个 skill 2 个键：description + short-description）
    - zh-CN catalog 文件未修改（本 Task 不触碰 zh-CN/，SHA 应与 16-WIP-SNAPSHOT.md 基线一致）
  </acceptance_criteria>

  <done>en/ catalog 已按键漂移审计结论更新（或确认无漂移，无需修改）</done>
</task>

<task type="auto">
  <name>Task 4: 更新非保护测试期望（runtime-locale-propagation、template-asset、response-language-docs）</name>
  <files>
    tests/runtime-locale-propagation.test.cjs
    tests/template-asset-localization.test.cjs
    tests/response-language-docs.test.cjs
  </files>

  <read_first>
    - tests/runtime-locale-propagation.test.cjs — 当前内容（读取后再修改）
    - tests/template-asset-localization.test.cjs — 当前内容（读取后再修改）
    - tests/response-language-docs.test.cjs — 当前内容（读取后再修改）
    - get-shit-done/workflows/plan-phase.md — 检查 response_language、canonical locale 等关键词是否存在
    - get-shit-done/workflows/discuss-phase.md — 同上
    - get-shit-done/workflows/execute-phase.md — 同上
    - get-shit-done/workflows/progress.md — 同上
    - docs/CONFIGURATION.md — 检查 BCP 47、"en"、"zh-CN"、alias 等关键词
    - get-shit-done/references/planning-config.md — 同上
    - .planning/phases/17-sync-localization-audit/17-PATTERNS.md — §`runtime-locale-propagation.test.cjs` 修复关注点
  </read_first>

  <action>
**策略：先运行各测试文件，读取失败信息确定具体哪些断言失败，再针对性修复。**

**Step 1：运行各测试，获取失败信息**
```bash
node --test tests/runtime-locale-propagation.test.cjs 2>&1 | grep -A 5 "AssertionError\|✗\|failed\|Error:"
node --test tests/template-asset-localization.test.cjs 2>&1 | grep -A 5 "AssertionError\|✗\|failed\|Error:"
node --test tests/response-language-docs.test.cjs 2>&1 | grep -A 5 "AssertionError\|✗\|failed\|Error:"
```

**Step 2：修复 runtime-locale-propagation.test.cjs**
根据测试失败信息和实际 workflow 文件内容，更新以下类型的断言：
- `assert.ok(content.includes('response_language'), ...)` — 读取实际 workflow 文件，确认关键词是否仍存在；若存在则断言正确，若不存在则 workflow 文件需补充（但不得修改 governance-surfaces.json）
- 若上游 workflow 文件格式变化导致某些检查字符串不再适用（例如格式从 `**Response language:**` 改为 `response_language:` 等），更新测试中的期望字符串以匹配新格式
- 保持语义契约：`response_language` 关键词必须存在于 priority workflow 文件中（这是 governance blocker 约束）

**Step 3：修复 template-asset-localization.test.cjs**
根据测试失败信息和实际文件内容，更新：
- assets catalog key 对等验证（en 和 zh-CN assets.json 键集应一致，如 zh-CN 缺失键则添加到 en 不能解决，需要检查 zh-CN/assets.json 是否有键漂移）
- workflow 文件内容关键词检查（`assets`、`fixed-string`、`response_language`、`English` 等）

**Step 4：修复 response-language-docs.test.cjs**
根据测试失败信息和实际文件内容，更新：
- BCP 47、`"en"`、`"zh-CN"` 示例检查 — 读取 docs/CONFIGURATION.md 和 get-shit-done/references/planning-config.md 的当前内容
- 若文档中这些示例已被上游删除，需要恢复这些文档条目（保持 locale 契约完整）
- 若测试期望的字符串格式发生变化（例如从单引号改双引号），更新测试期望

**Step 5：验证修复**
```bash
node --test tests/runtime-locale-propagation.test.cjs 2>&1 | tail -5
node --test tests/template-asset-localization.test.cjs 2>&1 | tail -5
node --test tests/response-language-docs.test.cjs 2>&1 | tail -5
```
  </action>

  <acceptance_criteria>
    - `node --test tests/runtime-locale-propagation.test.cjs 2>&1 | grep "fail"` 输出为空（0 failures）
    - `node --test tests/template-asset-localization.test.cjs 2>&1 | grep "fail"` 输出为空（0 failures）
    - `node --test tests/response-language-docs.test.cjs 2>&1 | grep "fail"` 输出为空（0 failures）
    - 三个文件均未修改 `response_language` 关键词的语义（只更新具体格式字符串）
    - governance-surfaces.json 未被修改（不是本 Task 的修改对象）
  </acceptance_criteria>

  <done>runtime-locale-propagation、template-asset-localization、response-language-docs 三个测试文件期望已更新，3 个文件均通过</done>
</task>

<task type="auto">
  <name>Task 5: 更新 install output 测试期望（6 个文件，依赖 Task 2 确认结论）</name>
  <files>
    tests/claude-install-output-localization.test.cjs
    tests/claude-install-output-fallback-boundary.test.cjs
    tests/claude-installer-locale-contract.test.cjs
    tests/claude-skill-display-catalog.test.cjs
    tests/codex-install-output-localization.test.cjs
    tests/codex-installer-locale-contract.test.cjs
  </files>

  <read_first>
    - tests/claude-install-output-localization.test.cjs — 当前内容（Phase 16 SHA-locked，Phase 17 允许有据修改）
    - tests/claude-install-output-fallback-boundary.test.cjs — 当前内容
    - tests/claude-installer-locale-contract.test.cjs — 当前内容
    - tests/claude-skill-display-catalog.test.cjs — 当前内容
    - tests/codex-install-output-localization.test.cjs — 当前内容
    - tests/codex-installer-locale-contract.test.cjs — 当前内容
    - bin/install.js — 当前 export 函数和 install 行为（确认函数签名和输出格式，重点：convertClaudeCommandToClaudeSkill 函数签名、输出格式）
    - get-shit-done/locales/zh-CN/claude-skills.json — 当前 zh-CN 值（不修改，但需要确认期望值与实际 catalog 一致）
    - .planning/phases/17-sync-localization-audit/17-PATTERNS.md — §`claude-install-output-localization.test.cjs` hardcoded 期望值部分
  </read_first>

  <action>
**前提：Task 2 已确认 PAIR_LEVEL_FALLBACK_OK=true。若 Task 2 发现 pair-level fallback 损坏，本 Task 不得执行（须先修复 bin/install.js）。**

**Step 1：运行各测试，读取具体失败信息**
```bash
node --test tests/claude-install-output-localization.test.cjs 2>&1 | grep -A 10 "AssertionError\|expected\|actual" | head -60
node --test tests/claude-install-output-fallback-boundary.test.cjs 2>&1 | grep -A 10 "AssertionError\|expected\|actual" | head -40
node --test tests/claude-installer-locale-contract.test.cjs 2>&1 | grep -A 10 "AssertionError\|expected\|actual" | head -40
node --test tests/claude-skill-display-catalog.test.cjs 2>&1 | grep -A 10 "AssertionError\|expected\|actual" | head -40
node --test tests/codex-install-output-localization.test.cjs 2>&1 | grep -A 10 "AssertionError\|expected\|actual" | head -40
node --test tests/codex-installer-locale-contract.test.cjs 2>&1 | grep -A 10 "AssertionError\|expected\|actual" | head -40
```

**Step 2：针对每个失败断言，应用 D-01 原则（跟上游走）**

常见失败模式及修复方法（来自 17-PATTERNS.md 和 POST-SYNC-REPORT 分析）：

**(a) hardcoded 输出字符串变化（install 进度文本）**
上游 bin/install.js 重构后，stdout 输出格式可能变化。修复：
- 运行实际安装 `GSD_TEST_MODE=1 node -e "..."` 捕获当前输出格式
- 将测试中旧的 `assert.match(output.stdout, /正在为/)` 等更新为新格式

**(b) 函数签名变化（convertClaudeCommandToClaudeSkill 参数）**
若函数签名发生变化，更新 require 和调用方式以匹配新 API。

**(c) skill body 字符串期望**
若测试检查 skill body 包含特定字符串（如 `'Create executable phase prompts (PLAN.md files) for a roadmap phase with integrated research and verification.'`），读取实际 bin/install.js 安装产出，更新期望字符串。

**(d) skill 枚举测试（claude-skill-display-catalog.test.cjs）**
此测试验证 en/claude-skills.json 覆盖所有 commands/gsd/ 下的 skill。若 Task 3 已更新 catalog，此测试应自动通过；若仍失败，检查键格式是否正确。

**Step 3：保留语义契约（不得弱化这些断言）**
无论如何更新，以下断言必须保留或加强：
- pair-level fallback 断言：partial zh-CN catalog 时整对回退 English（不得改为允许 mixed-language）
- English body 留存断言：skill body 必须保持 English canonical
- zh-CN NON_PROMISED_SAMPLE 边界：`tests/claude-install-output-fallback-boundary.test.cjs` 中的 `assertEnglishFallbackPair` 逻辑必须保留

**Step 4：验证所有 6 个文件通过**
```bash
node --test tests/claude-install-output-localization.test.cjs 2>&1 | tail -5
node --test tests/claude-install-output-fallback-boundary.test.cjs 2>&1 | tail -5
node --test tests/claude-installer-locale-contract.test.cjs 2>&1 | tail -5
node --test tests/claude-skill-display-catalog.test.cjs 2>&1 | tail -5
node --test tests/codex-install-output-localization.test.cjs 2>&1 | tail -5
node --test tests/codex-installer-locale-contract.test.cjs 2>&1 | tail -5
```

**Step 5：核查 zh-CN catalog 未被意外修改**
```bash
# 验证 zh-CN SHA 与 Phase 16 基线一致（或有明确的键漂移刷新依据）
sha256sum get-shit-done/locales/zh-CN/claude-skills.json
# 预期输出中包含：794b0edd72569ad630ded1bf2b7f4a91506931711d1c500a6b4d6357ff78c524
# 若 SHA 不匹配但本 Task 中未修改过该文件，说明意外修改，需回滚
```
  </action>

  <acceptance_criteria>
    - `node --test tests/claude-install-output-localization.test.cjs 2>&1 | grep "^fail\b"` 输出为空
    - `node --test tests/claude-install-output-fallback-boundary.test.cjs 2>&1 | grep "^fail\b"` 输出为空
    - `node --test tests/claude-installer-locale-contract.test.cjs 2>&1 | grep "^fail\b"` 输出为空
    - `node --test tests/claude-skill-display-catalog.test.cjs 2>&1 | grep "^fail\b"` 输出为空
    - `node --test tests/codex-install-output-localization.test.cjs 2>&1 | grep "^fail\b"` 输出为空
    - `node --test tests/codex-installer-locale-contract.test.cjs 2>&1 | grep "^fail\b"` 输出为空
    - `sha256sum get-shit-done/locales/zh-CN/claude-skills.json` 输出包含 `794b0edd72569ad630ded1bf2b7f4a91506931711d1c500a6b4d6357ff78c524`（zh-CN 未意外修改）
    - 所有 pair-level fallback 断言（`doesNotMatch /[\u4e00-\u9fff]/` 等）在各测试文件中仍存在
  </acceptance_criteria>

  <done>6 个 install output 和 locale contract 测试文件已更新，均通过；pair-level fallback 语义契约已保留</done>
</task>

<task type="auto">
  <name>Task 6: 更新 INVENTORY.md + INVENTORY-MANIFEST.json</name>
  <files>
    docs/INVENTORY.md
    docs/INVENTORY-MANIFEST.json
  </files>

  <read_first>
    - docs/INVENTORY.md — 当前内容（读取后再写入）
    - tests/inventory-counts.test.cjs — 了解格式约束（section 标题正则：`^##\s+{label}\s+\((\d+)\s+shipped\)`）
    - tests/inventory-manifest-sync.test.cjs — 了解 MANIFEST 驱动测试
    - scripts/gen-inventory-manifest.cjs — 了解生成命令和 JSON 结构
  </read_first>

  <action>
**Step 1：获取各 family 实际文件数（使用 Task 1 的结论）**
```bash
AGENTS_COUNT=$(ls agents/gsd-*.md 2>/dev/null | wc -l)
COMMANDS_COUNT=$(ls commands/gsd/*.md 2>/dev/null | wc -l)
WORKFLOWS_COUNT=$(ls get-shit-done/workflows/*.md 2>/dev/null | wc -l)
REFERENCES_COUNT=$(ls get-shit-done/references/*.md 2>/dev/null | wc -l)
CLI_MODULES_COUNT=$(ls get-shit-done/bin/lib/*.cjs 2>/dev/null | wc -l)
HOOKS_COUNT=$(ls hooks/*.js hooks/*.sh 2>/dev/null | wc -l)

echo "Agents: $AGENTS_COUNT"
echo "Commands: $COMMANDS_COUNT"
echo "Workflows: $WORKFLOWS_COUNT"
echo "References: $REFERENCES_COUNT"
echo "CLI Modules: $CLI_MODULES_COUNT"
echo "Hooks: $HOOKS_COUNT"
```

**Step 2：更新 docs/INVENTORY.md**

读取 docs/INVENTORY.md，针对各 section 标题进行以下操作：

对于每个 `## FamilyName (N shipped)` 标题：
- 将 N 更新为 Step 1 中对应的实际文件数
- 若实际文件数 > 当前记录数，说明有上游新增文件，在该 section 末尾追加新文件行

新文件行格式（参考现有行格式，保持一致性）。

**Step 3：重新生成 INVENTORY-MANIFEST.json**
```bash
node scripts/gen-inventory-manifest.cjs --write
echo "MANIFEST re-generated"
```

**Step 4：验证**
```bash
node --test tests/inventory-counts.test.cjs 2>&1 | tail -5
node --test tests/inventory-manifest-sync.test.cjs 2>&1 | tail -5
```
  </action>

  <acceptance_criteria>
    - `node --test tests/inventory-counts.test.cjs 2>&1 | grep "^fail\b"` 输出为空（0 failures）
    - `node --test tests/inventory-manifest-sync.test.cjs 2>&1 | grep "^fail\b"` 输出为空（0 failures）
    - `grep -E "^##.*\([0-9]+ shipped\)" docs/INVENTORY.md | wc -l` 输出 >= 6（6 个 family 标题均存在）
    - `node -e "const m = JSON.parse(require('fs').readFileSync('docs/INVENTORY-MANIFEST.json','utf8')); console.log(Object.keys(m.families).length)"` 输出 >= 6
    - docs/INVENTORY-MANIFEST.json 中 `generated` 字段为今天日期（2026-04-21）
  </acceptance_criteria>

  <done>INVENTORY.md 数字已更新，INVENTORY-MANIFEST.json 已重新生成，INVENTORY 相关测试全部通过</done>
</task>

<task type="auto">
  <name>Task 7: 写入 17-01-SUMMARY.md（审计结论与修复记录）</name>
  <files>
    .planning/phases/17-sync-localization-audit/17-01-SUMMARY.md
  </files>

  <read_first>
    - （Task 1 至 Task 6 的执行结论）
  </read_first>

  <action>
在 `.planning/phases/17-sync-localization-audit/17-01-SUMMARY.md` 中记录以下内容：

```markdown
# Phase 17 Plan 01: 审计+修复 SUMMARY

**完成时间：** [执行日期]
**执行结论：** PASS

## 键漂移审计结论（D-04 记录要求）

### claude-skills 键漂移
[记录新增/删除的 skill ID，或明确写入："无键漂移——commands/gsd/ 文件集与 en/claude-skills.json 键集完全一致"]

### codex-skills 键漂移
[同上，或 "无键漂移"]

### en/ catalog 修改摘要
[若无漂移：en/claude-skills.json 和 en/codex-skills.json 未修改]
[若有漂移：已新增 N 个键对]

## installer pair-level fallback 确认（D-02 执行结果）

- convertClaudeCommandToClaudeSkill 函数：存在 ✓
- resolveInstallerLocale 函数：存在 ✓
- zh-CN 完整路径：zh-CN catalog 正确 ✓
- pair-level fallback：partial catalog 时整对回退 English ✓ / ✗

## 测试修复摘要

| 测试文件 | 修复前状态 | 修复后状态 | 主要修复内容 |
|----------|-----------|-----------|------------|
| runtime-locale-propagation.test.cjs | FAIL | PASS | [具体修改说明] |
| template-asset-localization.test.cjs | FAIL | PASS | [具体修改说明] |
| response-language-docs.test.cjs | FAIL | PASS | [具体修改说明] |
| claude-install-output-localization.test.cjs | FAIL | PASS | [具体修改说明] |
| claude-install-output-fallback-boundary.test.cjs | FAIL | PASS | [具体修改说明] |
| claude-installer-locale-contract.test.cjs | FAIL | PASS | [具体修改说明] |
| claude-skill-display-catalog.test.cjs | FAIL | PASS | [具体修改说明] |
| codex-install-output-localization.test.cjs | FAIL | PASS | [具体修改说明] |
| codex-installer-locale-contract.test.cjs | FAIL | PASS | [具体修改说明] |

## INVENTORY 刷新摘要

| Family | 修复前 (N shipped) | 修复后 (N shipped) |
|--------|-------------------|-------------------|
| Agents | [旧数字] | [新数字] |
| Commands | [旧数字] | [新数字] |
| Workflows | [旧数字] | [新数字] |
| References | [旧数字] | [新数字] |
| CLI Modules | [旧数字] | [新数字] |
| Hooks | [旧数字] | [新数字] |

## zh-CN 受保护文件完整性核查

- zh-CN/claude-skills.json SHA: [实际 SHA] | 基线: 794b0edd... | 状态: [MATCH / CHANGED（有据修改）]
- zh-CN 其他文件：未修改 ✓

## 移交 Plan 02 前置条件

- [ ] 9 个失败测试文件全部通过
- [ ] INVENTORY 相关测试全部通过
- [ ] zh-CN 受保护文件完整性核查通过
```
  </action>

  <acceptance_criteria>
    - 文件 `.planning/phases/17-sync-localization-audit/17-01-SUMMARY.md` 存在
    - 文件包含 "键漂移审计结论" 章节（含明确结论或"无漂移"说明）
    - 文件包含 "pair-level fallback 确认" 章节
    - 文件包含 "测试修复摘要" 表格，覆盖全部 9 个测试文件
    - 文件包含 "INVENTORY 刷新摘要" 表格，覆盖全部 6 个 family
  </acceptance_criteria>

  <done>17-01-SUMMARY.md 写入完毕，Phase 17 Plan 01 审计与修复记录完整</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bin/install.js → test files | 测试文件的期望依赖 installer 行为，需在确认 pair-level fallback 正确后才能更新期望 |
| en/ catalog → tests | en/ catalog 键集变化会影响 claude-skill-display-catalog.test.cjs 和 codex 对应测试 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | zh-CN catalog（受保护） | mitigate | Task 5 结尾强制 SHA 验证；Task 3 只写 en/，不写 zh-CN/ |
| T-17-02 | Tampering | governance-surfaces.json（受保护） | mitigate | 本 Plan 不修改该文件；Task 4 的测试修复不涉及 manifest 结构变更 |
| T-17-03 | Integrity | pair-level fallback 语义弱化 | mitigate | Task 5 Step 3 明确要求保留所有 fallback 断言，不得删除 |
| T-17-04 | Integrity | mixed-language frontmatter 引入 | mitigate | Task 5 验证 `NON_PROMISED_SAMPLE` 边界断言必须保留 |
</threat_model>

<verification>
1. `node --test tests/runtime-locale-propagation.test.cjs` 退出码 0
2. `node --test tests/template-asset-localization.test.cjs` 退出码 0
3. `node --test tests/response-language-docs.test.cjs` 退出码 0
4. `node --test tests/claude-install-output-localization.test.cjs` 退出码 0
5. `node --test tests/claude-install-output-fallback-boundary.test.cjs` 退出码 0
6. `node --test tests/claude-installer-locale-contract.test.cjs` 退出码 0
7. `node --test tests/claude-skill-display-catalog.test.cjs` 退出码 0
8. `node --test tests/codex-install-output-localization.test.cjs` 退出码 0
9. `node --test tests/codex-installer-locale-contract.test.cjs` 退出码 0
10. `node --test tests/inventory-counts.test.cjs` 退出码 0
11. `node --test tests/inventory-manifest-sync.test.cjs` 退出码 0
12. `sha256sum get-shit-done/locales/zh-CN/claude-skills.json` 包含 `794b0edd72569ad630ded1bf2b7f4a91506931711d1c500a6b4d6357ff78c524`（或有据修改）
</verification>

<success_criteria>
- Task 1 键漂移审计完成，结论明确（无漂移/新增 N 个 skill）
- Task 2 pair-level fallback 确认通过（PAIR_LEVEL_FALLBACK_OK=true）
- Task 3 en/ catalog 已按审计结论更新（条件性）
- Task 4 3 个非保护测试文件通过
- Task 5 6 个 install/catalog 测试文件通过
- Task 6 INVENTORY 相关测试全部通过
- Task 7 17-01-SUMMARY.md 记录审计结论与修复摘要
</success_criteria>

<output>
完成后创建 `.planning/phases/17-sync-localization-audit/17-01-SUMMARY.md`，内容参见 Task 7。
</output>
